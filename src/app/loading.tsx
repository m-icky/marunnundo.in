import LogoLoader from '@/components/LogoLoader';

/** Next.js App Router uses this file automatically during route transitions */
export default function Loading() {
  return <LogoLoader variant="fullscreen" />;
}
